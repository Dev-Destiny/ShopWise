export interface Product {
    id?: string
    title: string
    description: string
    image: string
    price: number
    rating: number // 121, 400
    star?: number // 1 - 5
}

// title, description, image, price, rating